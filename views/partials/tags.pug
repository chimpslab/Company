
mixin tmpl_tags
    script#tmpl_tag(type="x-tmpl-mustache")
        a.bg-dark.text-light.text-monospace.p-1.text-capitalize(id="tag_{{.}}" href="#")
            label(for="tagcb_{{.}}")
                | {{.}}
                checkbox(id="tagcb_{{.}}" hidden)
    script#tmpl_tag_multi_select(type="x-tmpl-mustache")
        app-tag-select-multiple.tags-list.d-block.mt-1
            | {{#tags}}
            app-tag-checkbox.tag
                label.btn.btn-link.ml-1.p-1.text-uppercase.text-monospace.bg-dark.text-light(id="tag_{{.}}" for="tagcb_{{.}}")
                    | {{.}}
                    input.ml-1(id="tagcb_{{.}}" type="checkbox" value="{{.}}" )
            | {{/tags}}
    script#tmpl_ulist(type="x-tmpl-mustache")
        .tags-list.d-flex.flex-wrap
            | {{#tags}}
            span.badge.text-bg-light.position-relative.ms-2.mt-2.tag
                span {{.}}
                span.position-absolute.top-0.start-100.translate-middle
                    a.btn.btn-link.p-0(href="#" data-bs-dismiss=".tag" data-value="{{.}}")
                        .fa.fa-times-circle-o()

            | {{/tags}}
mixin input_tag
    app-tags
        label.control-label= _x("Tags", "Form Tags input")
        input.form-control(id='tagsin' placeholder="Separate values with ','")
        input.form-control(hidden, name='tags', id='tagsout' value="{{tags}}")
        small.col-sm-3.control-label.text-muted(data-tooltip) Separate values with ","